//! Cross-file call-site resolver consuming [`ix_code::semantic::CalleeHint`].
//!
//! Populated in step 1.4. Currently a placeholder so the crate skeleton
//! compiles under the module declarations in [`crate`].
