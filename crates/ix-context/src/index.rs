//! Project-wide symbol table built by a two-pass tree-sitter walk.
//!
//! Populated in step 1.3. Currently a placeholder so the crate skeleton
//! compiles under the module declarations in [`crate`].
